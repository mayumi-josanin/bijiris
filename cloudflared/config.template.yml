tunnel: REPLACE_WITH_TUNNEL_UUID
credentials-file: /Users/Kenmo/.cloudflared/REPLACE_WITH_TUNNEL_UUID.json

ingress:
  - hostname: app.example.com
    service: http://127.0.0.1:8123
  - service: http_status:404
